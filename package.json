{
  "name": "@janily/shipflow-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Codex MVP runner for the ShipFlow engineering workflow",
  "type": "module",
  "bin": {
    "shipflow": "./bin/shipflow.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check bin/shipflow.js && node --check src/*.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.149.0"
  }
}
